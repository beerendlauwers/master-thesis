var x;
var multiplier;
function fac(y)
{
    if( y == 0 )
        return 1;
    else
    { return y * multiplier * fac( y - 1 ); }
}

function main()
{
    multiplier = 2;
    x = fac( 3 );
    print x;
}